<%= form_for [@log, @log_entry] do |f| %>
<%= render 'layouts/form_errors', :errors => {:log_entry => @log_entry.errors} if @log_entry.errors.any? %>
<div class="clearfix">
  <div class="col-md-7 well well-lg">
    <p class="text-muted"><small>Z częstotliwością <%= @log_entry.mhz %> powiązane są poniższe stacje z bazy UKE. Jeśli uważasz, że
    znaleziona częstotliwość należy do którejś z nich, zaznacz właściwą.</small></p>
    <table class="table table-striped">
    <% @stations.each do |station| %>
      <tr>
        <td><%= station[:owner] %></td>
        <td><%= station[:display_name] %></td>
        <td><%= station[:net] %></td>
        <td><%= station[:distance].to_f.round(2) %>km</td>
        <td>
          <%= f.radio_button :frequency_assignment_id, station[:frequency_assignment_id] %>
        </td>
      </tr>
    <% end %>
    </table>
  </div>
  <div class="col-md-4 col-md-offset-1 well well-lg">


    <div class="form-group">
      <%= f.label :mhz, class: 'control-label' %>
      <%= f.text_field :mhz, class: 'form-control input-lg', readonly: true %>
    </div>

    <div class="form-group">
      <%= f.label :description, class: 'control-label' %>
      <%= f.text_area :description, class: 'form-control input-lg' %>
    </div>

    <div class="form-group">
      <%= f.label :level, class: 'control-label' %>
      <p class="text-muted"><small>1 - nieczytelny, 5 - doskonały</small></p>
      <%= f.number_field :level, class: 'form-control input-lg' %>
    </div>

    <div class="form-group">
      <%= f.submit class: 'btn btn-primary btn-lg' %>
    </div>
  </div>
</div>
<% end %>
